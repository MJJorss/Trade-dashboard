# The assets directory is the repo root, so without this the Worker source,
# its config and the git metadata are all published as public static files —
# and Wrangler's dev watcher reload-loops on .git churn.
.git
.gitignore
.assetsignore
node_modules
worker.js
wrangler.jsonc
README.md
